@model IEnumerable<SupplierRanking.Models.Fornecedor>

@{
    ViewBag.Title = "listaFuncionario";
}

<h2>listaFuncionario</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        
        <th>
            @Html.DisplayNameFor(model => model.Codigo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cnpj_fornecedor)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
       
        <td>
            @Html.DisplayFor(modelItem => item.Codigo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cnpj_fornecedor)
        </td>
        <td>
            @Html.ActionLink("Edit", "UpdateFuncionarioFornecedor", new {  item.Codigo }) |
            @Html.ActionLink("Details", "Details", new {  item.Codigo }) |
            @Html.ActionLink("Delete", "ExcluirFuncionario", new {  item.Codigo })
        </td>
    </tr>
}

</table>
